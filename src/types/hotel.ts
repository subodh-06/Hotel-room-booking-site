export interface Hotel {
    location: ReactNode;
    description: ReactNode;
    images: string | StaticImport;
    _id: string;
    name: string;
    city: string;
    address: string;
    pricePerNight: number;
    imageUrl: string;
  }
  